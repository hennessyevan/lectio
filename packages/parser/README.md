# @lectio/parser

This module is responsible for parsing the input file and converting it into a list of verses. The verses are then passed to the nlp module for further processing into verse parts.

## Usage

From the root @lectio directory, run the following command.

```bash
pnpm run parser
```
